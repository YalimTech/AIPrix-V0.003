// Jest setup file
import 'jest';

// Global test utilities
global.setTimeout = jest.setTimeout;
global.clearTimeout = jest.clearTimeout;
global.setInterval = jest.setInterval;
global.clearInterval = jest.clearInterval;
